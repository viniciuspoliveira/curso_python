salario_hora = float(input('Digite o seu salário por hora '))
horas_trabalhadas = float(input('Digite a quantidade de horas trabalhadas por mes '))
salario_total = salario_hora * horas_trabalhadas

print(f'O seu salário mensal é de: {salario_total} reais')